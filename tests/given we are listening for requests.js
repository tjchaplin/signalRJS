var assert = require('assert');

describe('Given we are listening for requests',function(){
	describe('When request url is connect',function(){
		it('should connect',function(){
			assert(true === false);
		});
	});
	describe('When request url is send',function(){
		it('should send',function(){
			assert(true === false);
		});
	});
	describe('When request url is negotiate',function(){
		it('should negotiate',function(){
			assert(true === false);
		});
	});
});